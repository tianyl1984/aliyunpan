# Binaries for programs and plugins
*.exe
*.dll
*.so
*.dylib
aliyunpan
aliyunpan.exe
cmd/AndroidNDKBuild/AndroidNDKBuild

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
out/
*.dl

# Project-local glide cache, RE: https://github.com/Masterminds/glide/issues/736
.glide/

# Others
.DS_Store
*.proc
*.txt
*.log
*.gz
captcha.png
aliyunpan_config.json
aliyunpan_command_history.txt
aliyunpan_uploading.json
test/
download/
*-downloading

# GoLand
.idea/

demos/
tests.sh
main_test.go
license_header.sh
account.txt
git_push.sh
git_pull.sh
docker/sync/data
clear_docker.sh
upload.sh
# webui 前端（fork 新增）
web/node_modules/
web/dist/
# 前端构建产物不提交，只保留 .gitkeep 占位。
# go:embed 在目录不存在或为空时会编译失败，占位文件保证 go build 能过；
# 但打包前必须先执行 ./build_web.sh，否则二进制里没有真正的前端资源。
internal/webui/assets/dist/*
!internal/webui/assets/dist/.gitkeep

# webui docker 部署的本地数据与口令，不提交
docker/webui/.env
docker/webui/config/
docker/webui/downloads/

# internal/log、internal/syncdrive 的测试里写死了 Windows 绝对路径（如
# internal/syncdrive/bolt_db_test.go:16）。在 macOS/Linux 上 \ 是普通字符，
# 整串会变成包目录下的一个文件名，跑 go test 就会留下这类垃圾文件。
# gitignore 里 \ 是转义符，匹配字面反斜杠必须写成 \\。
D:\\*
